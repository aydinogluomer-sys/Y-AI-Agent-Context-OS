node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env
.env.*

# [P19/T1] .env.test ISTISNA: entegrasyon testi yapilandirmasi GITE GIRER.
#
# Icindeki tek "sir" yerel konteynere ait (127.0.0.1:5433) ve disaridan
# erisilemez. Gite girmemesi, CI'in ve baska gelistiricilerin
# `test:integration` calistiramamasi demek olurdu.
#
# Uretim sirlari ASLA burada olmaz - secret manager'dan gelir (ADR-073).
!.env.test
!.env.example
.antigravity/
scratch/

# Secrets and Credentials
*.pem
*.key
secrets.json
credentials.json

# Test/build ciktilari
playwright-report/
test-results/
.vitest/

# ADR-014: tek paket yoneticisi pnpm. npm lockfile'i CI ile yerel arasinda
# sessiz surum sapmasi uretiyordu.
package-lock.json
yarn.lock

# P18: sentetik performans fixture'lari (deterministik, yeniden uretilebilir)
.perf/
.commitmsg.tmp
